package com.example.s1603459.myapplication

// User object (all fields added to Firebase under each user; each user is organized with their email as their unique "username")
class User(var userId: String, var name: String, var email: String, var level: Int)